---
version: '2'
services:
  lempemp:
    labels:
      rap.host: "${DOMAIN}"
      rap.port: 80
      rap.proto: http
      rap.le_host: "${DOMAIN}"
      rap.le_email: "divo@divo.net.pl"
    image: stenote/docker-lemp
    volumes:
      - lemp-data:/var/www/

volumes:
  lemp-data: